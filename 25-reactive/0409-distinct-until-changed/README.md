# 0409 — Distinct Until Changed

Implement distinctUntilChanged, dropping consecutive duplicate values from 1,1,2,2,2,3,1. Track the last forwarded value in a closure and use EqualityComparer<T>.Default to compare.

## Run

    dotnet run
