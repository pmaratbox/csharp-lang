# 0526 — Add time

Using NodaTime, parse the fixed datetime `2026-06-15T10:00` into a `LocalDateTime`, add 90 minutes with `PlusMinutes`, and format the resulting time-of-day as `HH:mm`, yielding `11:30`.

## Run

    dotnet run
