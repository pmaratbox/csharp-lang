# 0583 — Select the &lt;h1&gt; tag

Uses the AngleSharp library to parse a fixed HTML document and query it with the CSS type selector `h1`. `QuerySelector("h1")` returns the first matching element, and its `TextContent` is printed to produce `Hello`.

## Run

    dotnet run
