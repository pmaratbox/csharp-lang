# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Recursive bound checks read cleanly as a single boolean expression in C#.

## Run

    dotnet run
