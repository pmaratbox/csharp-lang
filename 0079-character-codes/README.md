# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. Casting `'A'` to `int` gives its UTF-16 code; casting `66` to `char` gives the character for that code.

## Run

    dotnet run
