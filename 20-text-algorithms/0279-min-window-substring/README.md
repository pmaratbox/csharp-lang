# 0279 — Minimum Window Substring

Find the smallest window in "ADOBECODEBANC" containing all of "ABC", printing `BANC`. A `Dictionary<char,int>` sliding window tracks how many required counts are satisfied.

## Run

    dotnet run
