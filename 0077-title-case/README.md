# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. `Split` breaks the words; `char.ToUpper(w[0])` uppercases each first letter, concatenated with the rest via `Substring(1)`.

## Run

    dotnet run
