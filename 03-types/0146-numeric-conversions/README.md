# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. A `(int)` cast truncates toward zero while widening an `int` to `double` is implicit; `"0.0"` with `InvariantCulture` formats one decimal place.

## Run

    dotnet run
