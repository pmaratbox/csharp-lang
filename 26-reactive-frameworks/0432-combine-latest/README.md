# 0432 — Combine Latest

Use the library's combineLatest operator on a virtual/test scheduler, emitting the latest pair whenever either timed source emits. Built with System.Reactive (Rx.NET) using `CombineLatest` driven by a `TestScheduler`.

## Run

    dotnet run
