# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. C# composite formatting with `D2`/`D4` specifiers under `InvariantCulture` zero-pads each component and joins them with dashes.

## Run

    dotnet run
