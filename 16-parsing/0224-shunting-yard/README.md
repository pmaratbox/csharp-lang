# 0224 — Shunting Yard

Convert the infix expression "3 + 4 * 2" to postfix `3 4 2 * +`. A `Stack<string>` of operators with a precedence helper implements Dijkstra's algorithm.

## Run

    dotnet run
