# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. `Stack<char>` pushes on `(` and pops on `)`; an empty stack at the end means the brackets balanced.

## Run

    dotnet run
