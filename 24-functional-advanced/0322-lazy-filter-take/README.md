# 0322 — Lazy Filter + Take

From a lazy stream of naturals, filter the even ones and take three, printing `2 4 6`. LINQ's `Where` and `Take` stay lazy over the infinite iterator.

## Run

    dotnet run
