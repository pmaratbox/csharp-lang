# 0584 — Select by id

Uses the AngleSharp library to parse the fixed HTML document and query it with the CSS id selector `#status`. The matched `<span id="status">` element's `TextContent` is printed, producing `active`.

## Run

    dotnet run
