# 0330 — Lens Get/Set

Use a lens over the nested value {a:{b:1}} to get b (1) and to set b to 2, printing `1 2`. A `Lens` record pairs a getter with a `with`-based immutable setter.

## Run

    dotnet run
