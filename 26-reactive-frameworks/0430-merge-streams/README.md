# 0430 — Merge Streams

Use the library's merge operator on a virtual/test scheduler to interleave two timed streams by emission time. Uses System.Reactive (Rx.NET) `Observable.Merge` over a `TestScheduler`.

## Run

    dotnet run
