# 0129 — Add Days to a Date

Add 40 days to 2000-01-01 and print the result as `2000-02-10`. The civil-from-days inverse returns a tuple that C# destructures with `var (y, m, d)` before zero-padded `D2`/`D4` formatting under `InvariantCulture`.

## Run

    dotnet run
