# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Uses System.Reactive (Rx.NET) and its `Zip` operator to pair two synchronous `IObservable<int>` streams by index.

## Run

    dotnet run
