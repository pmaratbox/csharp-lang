# 0101 — Threads: Spawn and Join

Spawn 3 worker threads, wait for all of them to finish, then print `done: 3`. Each `Thread` is started then `Join`ed in turn before printing the count.

## Run

    dotnet run
