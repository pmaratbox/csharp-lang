# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. `string.PadLeft` and `string.PadRight` handle width-padding directly.

## Run

    dotnet run
