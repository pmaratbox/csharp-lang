# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. C# expression-bodied static local functions keep the rule `year % 4 == 0 && (year % 100 != 0 || year % 400 == 0)` compact and reusable.

## Run

    dotnet run
