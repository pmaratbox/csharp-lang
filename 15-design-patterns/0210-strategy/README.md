# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Strategies are `Func<int,int,int>` lambdas chosen by name in a `switch` expression.

## Run

    dotnet run
