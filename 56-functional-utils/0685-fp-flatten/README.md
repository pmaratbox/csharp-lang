# 0685 — Flatten

This lesson uses C#'s LINQ (the standard collection library) and its `SelectMany` operator to flatten a list of lists one level deep. Each inner sequence is projected and concatenated into a single flat sequence, which is then comma-joined for output.

## Run

    dotnet run
