# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. A `Regex.Replace` with a match evaluator looks each `{key}` up in a dictionary.

## Run

    dotnet run
