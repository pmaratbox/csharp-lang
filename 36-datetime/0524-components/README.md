# 0524 — Date components

Using NodaTime, parse the fixed date `2026-06-15` with `LocalDatePattern.Iso` and read its components via the library's accessors `Year`, `Month`, and `Day`, printing each on its own line.

## Run

    dotnet run
