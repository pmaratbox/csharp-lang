# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. `string.Contains` on the doubled string captures the rotation invariant in one line.

## Run

    dotnet run
