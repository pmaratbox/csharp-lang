# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. A `Dictionary` captured by the local function caches results; `TryGetValue` returns a hit before recomputing.

## Run

    dotnet run
