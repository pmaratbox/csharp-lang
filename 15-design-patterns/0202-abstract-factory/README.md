# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A single `IThemeFactory` produces a family of related widgets whose labels share the theme prefix.

## Run

    dotnet run
