# 0357 — Zip Longest

Zip [1,2,3] with ["a","b"], filling the missing slot with "-", printing `1a 2b 3-`. Iterate to the longer length and substitute "-" via a ternary when an index falls past the shorter array.

## Run

    dotnet run
