# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A small helper throws on a false condition, so reaching the end proves every check held.

## Run

    dotnet run
