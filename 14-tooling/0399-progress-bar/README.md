# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. The `new string(char, count)` constructor builds the filled and empty segments idiomatically.

## Run

    dotnet run
