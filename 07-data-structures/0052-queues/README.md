# 0052 — Queues

Enqueue `1`, `2`, and `3` into a queue, then dequeue them all and print them in first-in-first-out order: `1 2 3`. `System.Collections.Generic.Queue<T>` is the FIFO queue: `Enqueue` adds at the back and `Dequeue` removes and returns the front.

## Run

    dotnet run
