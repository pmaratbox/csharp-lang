# 0435 — SwitchMap

Use the library's switchMap on a virtual/test scheduler so a new outer value cancels the previous inner stream. Built with System.Reactive (Rx.NET) using `Select(...).Switch()` on a `TestScheduler`.

## Run

    dotnet run
