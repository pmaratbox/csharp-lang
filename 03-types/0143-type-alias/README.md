# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. A C# `using` alias gives `System.Int32` the name `Meters` for readability while staying the same underlying value type.

## Run

    dotnet run
