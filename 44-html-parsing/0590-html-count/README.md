# 0590 — Count matching elements

Uses the AngleSharp library to parse the fixed HTML document and count how many elements match the CSS class selector `.item`. `QuerySelectorAll(".item")` returns every matching `<li>`, and its `.Length` prints the integer `3`.

## Run

    dotnet run
