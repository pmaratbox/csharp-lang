# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Overriding `ToString()` controls how `Console.WriteLine` renders the object.

## Run

    dotnet run
