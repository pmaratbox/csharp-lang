# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. A static method on the record acts as a named constructor, parsing hex pairs with `NumberStyles.HexNumber`.

## Run

    dotnet run
