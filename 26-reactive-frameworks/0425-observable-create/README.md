# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses System.Reactive (Rx.NET) with `ToObservable` to emit the array and an `onCompleted` callback for done.

## Run

    dotnet run
