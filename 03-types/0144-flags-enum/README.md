# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. A `[Flags]` enum lets you OR members together and test membership with a bitwise `&`.

## Run

    dotnet run
