# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. Modular rolling hashes are compared, then verified with `Substring` on a hash hit.

## Run

    dotnet run
