# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Early returns handle the special cases first, keeping the local function flat.

## Run

    dotnet run
