# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. In C# a `catch` for a base exception type matches any derived exception.

## Run

    dotnet run
