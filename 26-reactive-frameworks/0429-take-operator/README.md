# 0429 — Take Operator

Use the library's take operator to take the first 3 values of an unbounded stream, then complete. Uses System.Reactive (Rx.NET) with `Observable.Generate` for the unbounded source and the `Take` operator.

## Run

    dotnet run
