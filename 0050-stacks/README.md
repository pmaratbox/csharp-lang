# 0050 — Stacks

Push `1`, `2`, and `3` onto a stack, then pop them all off and print them in last-in-first-out order: `3 2 1`. `Stack<T>` is the dedicated LIFO type with `Push`, `Pop`, and `Peek`. `Pop` both removes and returns the top element.

## Run

    dotnet run
