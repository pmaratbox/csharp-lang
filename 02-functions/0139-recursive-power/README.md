# 0139 — Recursive Power

Compute 2 raised to the 10th power recursively and print `1024`. The base case `exp == 0` returns 1, expressed with a C# expression-bodied recursive method.

## Run

    dotnet run
