# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Range slicing strips the brackets and LINQ's `Sum(int.Parse)` totals the elements.

## Run

    dotnet run
