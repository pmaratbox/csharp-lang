# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. `string.IndexOf` with an advancing index scans for non-overlapping matches.

## Run

    dotnet run
