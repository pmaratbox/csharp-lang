# 0427 — Filter Operator

Use the library's filter operator to keep the even values of 1..6. Uses System.Reactive (Rx.NET) with the `Where` operator (Rx.NET's name for filter).

## Run

    dotnet run
