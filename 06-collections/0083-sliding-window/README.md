# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. `Skip(i).Take(size)` extracts each window; LINQ has no built-in sliding-window operator.

## Run

    dotnet run
