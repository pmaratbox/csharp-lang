# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. C# enums are integer-backed, so casting to and from `int` gives the ordinal and the name at that position.

## Run

    dotnet run
