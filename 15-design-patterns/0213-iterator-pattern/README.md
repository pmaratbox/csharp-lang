# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A hand-written `HasNext()`/`Next()` pair walks the range and the values are joined with `string.Join`.

## Run

    dotnet run
