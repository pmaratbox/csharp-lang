# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Uses System.Reactive (Rx.NET) and its `Concat` operator to join two synchronous `IObservable<int>` streams in order.

## Run

    dotnet run
