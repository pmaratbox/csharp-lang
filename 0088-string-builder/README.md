# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. `System.Text.StringBuilder` is the mutable builder; `Append` adds each piece without allocating a new string.

## Run

    dotnet run
