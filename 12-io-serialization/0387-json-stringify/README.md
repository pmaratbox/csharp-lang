# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. C# builds the compact string with an interpolated string to guarantee no extra spaces.

## Run

    dotnet run
