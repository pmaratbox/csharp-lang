# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A static auto-property incremented in the constructor holds the class-level count.

## Run

    dotnet run
