# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. Build runs in a single pass, comparing each element to the last element of the current run.

## Run

    dotnet run
