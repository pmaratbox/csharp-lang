# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each decorator implements `ICoffee` and delegates to the inner component, adding to its `Cost()`.

## Run

    dotnet run
