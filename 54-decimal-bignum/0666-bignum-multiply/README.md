# 0666 — Big integer multiplication

Multiply two large integers using `System.Numerics.BigInteger`, whose `*`
operator computes the exact product of arbitrary-precision integers — here
`123456789 * 987654321` with no overflow or rounding.

## Run

    dotnet run
