# 0198 — Seeded LCG PRNG

Implement a linear congruential generator next=(5*x+3) mod 16 seeded at 1 and print its first 3 outputs `8 11 10`. C# computes the recurrence by hand with the `%` operator rather than the built-in `Random`.

## Run

    dotnet run
