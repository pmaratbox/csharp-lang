# 0585 — Select by class

Uses the AngleSharp library to parse a fixed HTML document and query it with the CSS class selector `.item`. `QuerySelector` returns the first matching element, whose `TextContent` is printed as `apple`.

## Run

    dotnet run
