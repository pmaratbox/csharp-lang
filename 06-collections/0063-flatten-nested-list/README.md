# 0063 — Flatten Nested List

Flatten the nested list `[[1, 2], [3, 4]]` into a single flat sequence and print its elements: `1 2 3 4`. LINQ's `SelectMany(row => row)` projects and concatenates each inner sequence, flattening one level.

## Run

    dotnet run
