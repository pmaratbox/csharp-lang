# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. The prefix-function is built with idiomatic C# arrays and a single forward scan.

## Run

    dotnet run
