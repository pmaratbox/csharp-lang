# 0222 — Bracket Matching

Check that mixed brackets are balanced: "([{}])" (yes) and "([)]" (no), printing `yes no`. A `Stack<char>` pushes opens and matches each close against its pair.

## Run

    dotnet run
