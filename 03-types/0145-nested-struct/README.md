# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. A C# `struct` can hold another `struct` by value, so `person.Address.City` reaches the nested field directly.

## Run

    dotnet run
