# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. C# enumerates an enum's member names in declaration order via `Enum.GetNames<T>()`.

## Run

    dotnet run
