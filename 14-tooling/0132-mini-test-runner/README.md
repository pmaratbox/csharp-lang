# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is an `Action` closure run inside a try/catch that tallies passes and failures.

## Run

    dotnet run
