# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. C# uses `try` / `catch` to handle a thrown exception and a `finally` block that always executes.

## Run

    dotnet run
