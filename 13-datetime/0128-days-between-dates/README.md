# 0128 — Days Between Dates

Compute the number of days from 2000-01-01 to 2000-12-31 and print `365`. A static local `DaysFromCivil` helper returns a `long` day-count so the two dates simply subtract.

## Run

    dotnet run
