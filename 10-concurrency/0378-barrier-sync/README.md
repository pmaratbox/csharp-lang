# 0378 — Barrier Synchronization

Have 3 threads each arrive at a barrier before any proceeds, then print `all reached: 3`. C# `Barrier` runs a post-phase action once all participants signal and wait.

## Run

    dotnet run
