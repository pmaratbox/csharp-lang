# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. A lazy `yield` iterator repeatedly applies the function and `Take` limits the run.

## Run

    dotnet run
