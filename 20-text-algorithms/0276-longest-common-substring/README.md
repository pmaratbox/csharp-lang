# 0276 — Longest Common Substring

Find the longest common contiguous substring of "abcde" and "xbcdy", printing `bcd`. A DP table over suffix matches tracks the best run, sliced with `Substring`.

## Run

    dotnet run
