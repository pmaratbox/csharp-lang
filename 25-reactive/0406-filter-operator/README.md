# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. C# closures wrap the producer and observer so the operator simply gates each `Next` call through the predicate.

## Run

    dotnet run
