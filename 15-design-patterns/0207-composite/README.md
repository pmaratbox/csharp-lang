# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. Leaves and composites share `IComponent`, so a composite sums its children via LINQ `Sum`.

## Run

    dotnet run
