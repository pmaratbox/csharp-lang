# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Shape` abstraction holds an `IColor` implementor, decoupling the two hierarchies.

## Run

    dotnet run
