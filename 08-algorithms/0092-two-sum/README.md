# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A `Dictionary` of value to index; `TryGetValue(target - nums[i], ...)` finds an earlier complement in a single pass.

## Run

    dotnet run
