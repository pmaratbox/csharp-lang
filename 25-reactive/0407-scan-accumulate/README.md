# 0407 — Scan (Running Fold)

Implement a scan operator that emits the running accumulation; produce the running sums of 1, 2, 3, 4. A generic `Observable<T>` with closure-based observers makes the operator chain read fluently.

## Run

    dotnet run
