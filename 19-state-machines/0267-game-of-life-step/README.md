# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. C# pattern matching (`is 2 or 3`) makes the Conway neighbour rules read naturally.

## Run

    dotnet run
