# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `Array.Sort` orders each `char[]`, and LINQ's `SequenceEqual` compares them element by element.

## Run

    dotnet run
