# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. C# uses `File.WriteAllLines` and `File.ReadAllLines` for one-call file round-trips.

## Run

    dotnet run
