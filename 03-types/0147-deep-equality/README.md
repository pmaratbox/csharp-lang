# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. C# `record` types give value-based `==` that recurses into nested records automatically.

## Run

    dotnet run
