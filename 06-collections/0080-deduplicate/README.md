# 0080 — Deduplicate

Remove duplicates from `1, 2, 2, 3, 1`, keeping the first occurrence of each in order, and print `1 2 3`. LINQ's `Distinct()` yields each value once in first-seen order.

## Run

    dotnet run
