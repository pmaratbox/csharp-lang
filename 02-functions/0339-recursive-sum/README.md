# 0339 — Recursive List Sum

Recursively sum the list [1,2,3,4], printing `10`. C# slices a ReadOnlySpan with a range so each recursive call drops the head without allocating.

## Run

    dotnet run
