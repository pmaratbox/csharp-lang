# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A `static` method on a `static class` is called as `MathUtil.Square(...)` with no instance.

## Run

    dotnet run
