# 0686 — Unique

This lesson uses C#'s LINQ (the standard collection library) and its `Distinct` operator to remove duplicate elements while preserving first-seen order. The deduplicated sequence is then comma-joined for output.

## Run

    dotnet run
