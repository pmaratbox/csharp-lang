# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A `Dictionary` lookup gives the deterministic successor for each state.

## Run

    dotnet run
