# 0217 — Memento

Save state 1, change it to 2, then restore from the memento, printing current then restored `2 1`. An immutable `record` memento captures the originator's state for later restoration.

## Run

    dotnet run
