# 0164 — Merge Maps

Merge {a:1,b:2} and {b:3,c:4} with the right map winning on conflicts, printing `a:1 b:3 c:4`. Copy the left dictionary then overwrite with the right via indexer assignment.

## Run

    dotnet run
