# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. C# composes behavior by implementing multiple interfaces whose default methods act as mixed-in traits.

## Run

    dotnet run
