# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Each recursive call appends to a list, which is then joined with spaces via `string.Join`.

## Run

    dotnet run
