# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. An iterative bottom-up array layout makes the range query a tight bit-shifting loop.

## Run

    dotnet run
