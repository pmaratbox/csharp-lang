# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A static factory maps names to types via a `switch` expression returning the `IShape` interface.

## Run

    dotnet run
