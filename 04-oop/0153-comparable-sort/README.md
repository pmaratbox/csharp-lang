# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. Implementing `IComparable<T>` lets `List.Sort` order instances by age.

## Run

    dotnet run
