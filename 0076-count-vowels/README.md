# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. LINQ's `Count` with a predicate tallies the characters for which `vowels.Contains(ch)` is true.

## Run

    dotnet run
