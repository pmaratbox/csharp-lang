# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. A local expand-around-center function updates the best span as it scans.

## Run

    dotnet run
