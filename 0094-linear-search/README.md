# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. `Array.IndexOf` performs the linear scan and returns the first matching index (or `-1`).

## Run

    dotnet run
