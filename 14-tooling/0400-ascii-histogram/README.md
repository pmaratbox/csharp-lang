# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. Repeating a char with `new string('#', n)` prints each bar in one statement.

## Run

    dotnet run
