# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. LINQ `Select` with the ternary `n % 2 == 0` maps each number to its label.

## Run

    dotnet run
