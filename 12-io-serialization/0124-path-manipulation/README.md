# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. C# uses `Path.GetFileName` and `Path.GetExtension` with "/" separators.

## Run

    dotnet run
