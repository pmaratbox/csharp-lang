# 0151 — Singleton

Obtain a singleton instance twice and confirm both references are the same object, printing `same: yes`. A static `Lazy<T>` gives a thread-safe, lazily created single instance.

## Run

    dotnet run
