# 0140 — Tower of Hanoi

Count the moves needed to solve Tower of Hanoi for 3 disks recursively and print `7`. The recurrence `moves(n) = 2*moves(n-1) + 1` is written as a C# expression-bodied recursive method.

## Run

    dotnet run
