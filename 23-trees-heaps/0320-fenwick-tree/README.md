# 0320 — Fenwick Tree Prefix Sum

Build a Fenwick (BIT) tree over [1,2,3,4,5] and query the prefix sum of the first 4 elements, printing `10`. The low-bit trick `i & -i` drives both update and prefix loops idiomatically.

## Run

    dotnet run
