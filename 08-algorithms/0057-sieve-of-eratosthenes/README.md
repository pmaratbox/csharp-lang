# 0057 — Sieve of Eratosthenes

Use the Sieve of Eratosthenes to find every prime number up to `10` and print them: `2 3 5 7`. `Array.Fill` seeds the `bool[]` true; multiples are struck from `i*i`, and the survivors are gathered in a second pass.

## Run

    dotnet run
