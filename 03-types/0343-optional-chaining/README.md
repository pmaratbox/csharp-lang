# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. C# chains the `?.` null-conditional operator with `??` to supply the fallback.

## Run

    dotnet run
