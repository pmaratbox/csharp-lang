# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. C# composes `Task` continuations with `ContinueWith` and awaits the final result.

## Run

    dotnet run
