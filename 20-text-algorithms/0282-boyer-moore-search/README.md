# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. A `Dictionary<char,int>` of last occurrences drives the bad-character shift.

## Run

    dotnet run
