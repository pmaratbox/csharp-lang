# 0689 — Take

Uses LINQ (the .NET standard query library) and its `Take` operator to take the first N elements of a sequence. Here `Enumerable.Range(1, 10).Take(3)` yields the first 3 elements of `[1..10]`, joined with commas.

## Run

    dotnet run
