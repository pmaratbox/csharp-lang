# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. A `List<int>` backs the heap with manual sift-up on push and sift-down on pop, so popping all elements yields them in ascending order.

## Run

    dotnet run
