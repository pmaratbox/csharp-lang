# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. Zip the two arrays into pairs and `SelectMany` to flatten them back into one alternating sequence.

## Run

    dotnet run
