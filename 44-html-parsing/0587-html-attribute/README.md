# 0587 — Read an attribute

Uses the AngleSharp library to parse a fixed HTML document, then selects the `<a>` element with the CSS tag selector `a` via `QuerySelector` and reads its `href` attribute with `GetAttribute("href")` to print `https://example.com`.

## Run

    dotnet run
