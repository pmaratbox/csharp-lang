# 0073 — Caesar Cipher

Encrypt `abc` with a Caesar cipher shifting each letter forward by `1` (wrapping within the alphabet) and print the result: `bcd`. `ch - 'a'` promotes to `int`; LINQ `Select` maps each shifted character, and a new `string` is built from the result.

## Run

    dotnet run
