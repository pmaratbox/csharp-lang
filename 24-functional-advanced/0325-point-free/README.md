# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. A generic `Compose` helper builds the function from `sum` and `mapSquare` without naming the argument.

## Run

    dotnet run
