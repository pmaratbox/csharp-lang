# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. C# models the sum type with an abstract record hierarchy and a recursive switch expression.

## Run

    dotnet run
