# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. C#'s `string.PadRight` makes column alignment a one-liner.

## Run

    dotnet run
