# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. Splitting on whitespace (RemoveEmptyEntries) and on newlines gives the three counts.

## Run

    dotnet run
