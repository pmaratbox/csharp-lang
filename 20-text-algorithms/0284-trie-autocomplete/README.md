# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. A `Dictionary`-backed `TrieNode` is descended to the prefix, then DFS collects words in key order.

## Run

    dotnet run
