# 0308 — Strongly Connected Components

Count the strongly connected components of 0->1,1->2,2->0,2->3, printing `2`. Tarjan's algorithm uses a recursive local function with index/low-link arrays and an explicit `Stack<int>`.

## Run

    dotnet run
