# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. In C# a private field is exposed through a read-only property getter.

## Run

    dotnet run
