# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `Array.Reverse` flips a `char[]` (UTF-16 units) and a new `string` is built from it; non-BMP surrogate pairs would need extra care.

## Run

    dotnet run
