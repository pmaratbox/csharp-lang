# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `Array.Reverse` on the `char[]` builds the mirror, and a new `string` from it is compared to the original.

## Run

    dotnet run
