# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. A C# collection-expression `string[]` indexed by the congruence result maps the integer cleanly to a full weekday name.

## Run

    dotnet run
