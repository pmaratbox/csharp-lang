# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Each `Node` carries nullable `Prev`/`Next` references, so walking either direction is a simple `for` loop until null.

## Run

    dotnet run
