# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. `Skip(k)` (tail) concatenated with `Take(k)` (head) yields the left rotation via LINQ.

## Run

    dotnet run
