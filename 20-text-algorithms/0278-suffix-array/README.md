# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Suffix start indices are sorted by `CompareOrdinal` on range-sliced suffixes.

## Run

    dotnet run
