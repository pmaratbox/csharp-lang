# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A `record struct` carries the data while a custom `ToString` override yields the exact structured form.

## Run

    dotnet run
